# Auto-generated by rebuild_inits.py — do not hand-edit.
# Re-exports selected public symbols lazily to preserve import time.
from importlib import import_module as _im
from typing import Any as _Any

__all__ = [
    'EnhancedXMLBuilder',
    'children',
    'count_elements',
    'find_elements_by_tag',
    'is_element',
    'iter_descendants',
    'walk',
    'enhanced_builder',
    'safe_iter'
]

# map symbol -> relative module
_symbol_map = {'EnhancedXMLBuilder': 'enhanced_builder', 'is_element': 'safe_iter', 'count_elements': 'safe_iter', 'find_elements_by_tag': 'safe_iter', 'walk': 'safe_iter', 'children': 'safe_iter', 'iter_descendants': 'safe_iter'}
_module_map = {'enhanced_builder': 'enhanced_builder', 'safe_iter': 'safe_iter'}


def __getattr__(name: str) -> _Any:  # PEP 562
    mod = _symbol_map.get(name)
    if mod is not None:
        module = _im(f".{mod}", __name__)
        return getattr(module, name)

    pkg = _module_map.get(name)
    if pkg is not None:
        return _im(f".{pkg}", __name__)

    raise AttributeError(f"module {__name__} has no attribute {name!r}")


def __dir__():
    return sorted(set(globals().keys()) | set(__all__))
